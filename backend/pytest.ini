[pytest]
testpaths = tests
pythonpath = .
addopts = -q --strict-markers
markers =
    equivalence: proves an ONNX graph matches the native model it was exported from
    slow: takes more than a second
