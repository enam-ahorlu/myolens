# User-acceptance test run against the live deployment

Executed 15 August 2026, 18:22 UTC, by `scripts/run_uat.py --markdown` against
`https://myolens-api-ta5ir46azq-ew.a.run.app`, signed in as the examiner account over Firebase
Authentication. Every request crossed the network and went through Cloud Run, Cloud Storage and
Firestore -- nothing here is a fake or an emulator.

**17 passed, 0 failed, 5 manual, 22 cases**

A case that cannot be driven from an HTTP client is reported as `MANUAL` with the reason and the
place it *is* checked, rather than quietly omitted. Two of the five are browser behaviour, one
needs a second clinician's credentials, one is a thirty-minute wall clock, and one -- E2/FR-07 --
is a presentation requirement that an earlier draft of this suite wrongly asserted against the
API. See the note on that row.

## Console output

```

Access control
  PASS   A1/I2     An unauthenticated request is refused
  PASS   I2        A malformed bearer token is refused
  PASS   A2        A clinician calling an admin route is forbidden
  MANUAL A3        A clinician cannot read another clinician's participant
  MANUAL A5        Thirty-minute idle timeout signs the operator out

Input validation
  PASS   I1        A malformed payload is rejected, not a stack trace
  PASS   B1        No name field exists; one supplied is not stored

Refusals that define the product
  PASS   D3        A non-conformant montage is refused, naming the violations
  PASS   E7        Metrics for an unknown session are refused
  PASS   C4        A recording outside the training distribution is refused

The approval gate, on real data
  PASS   C1/C2     A conformant calibration is accepted and scored per task
  PASS   D1/D2     A session recording is accepted
  PASS   D4-D8     Segmentation produces bouts, with the doubtful ones flagged
  MANUAL E2/FR-07  The review list is ordered least-certain first
  PASS   E7/F1     No metric is computed before a human approves
  PASS   G1/E7     Export is refused before approval too
  PASS   E7        An operator can approve the segmentation
  PASS   F1/F4     Metrics are computed once approved, with correction rate and confidence

Provenance and the model card
  PASS   H1/FR-09  The model card names both accuracy regimes
  PASS   H2/FR-10  Every inference records the model version that produced it
  MANUAL F3        The intended-use statement appears on every screen and in the export
  MANUAL I5/I6     Responsive to 768 px, WCAG 2.1 AA contrast

17 passed, 0 failed, 5 manual, 22 cases
```

## Test log

| # | Req | Test case | Expected | Actual | Result |
|---|---|---|---|---|---|
| 1 | A1/I2 | An unauthenticated request is refused | 401 | 401 | **PASS** |
| 2 | I2 | A malformed bearer token is refused | 401 | 401 | **PASS** |
| 3 | A2 | A clinician calling an admin route is forbidden <br/><sub>A clinician account is expected here; 200 would mean the account holds admin.</sub> | 403 | 403 | **PASS** |
| 4 | A3 | A clinician cannot read another clinician's participant <br/><sub>Needs a second account's credentials. Covered by the Firestore rules suite (12 tests, real emulator) and by backend/tests/test_participants.py.</sub> | 404 | not automated | **MANUAL** |
| 5 | A5 | Thirty-minute idle timeout signs the operator out <br/><sub>Wall-clock behaviour in the browser; unit-tested in frontend/src/lib/idleTimeout.test.ts.</sub> | Signed out after 30 minutes idle | not automated | **MANUAL** |
| 6 | I1 | A malformed payload is rejected, not a stack trace | 422 | 422 | **PASS** |
| 7 | B1 | No name field exists; one supplied is not stored | 201 and no name in the record | 201, name present: False | **PASS** |
| 8 | D3 | A non-conformant montage is refused, naming the violations <br/><sub>3 violation(s) named</sub> | 409 montage_rejected | 409 montage_rejected | **PASS** |
| 9 | E7 | Metrics for an unknown session are refused | 404 | 404 not_found | **PASS** |
| 10 | C4 | A recording outside the training distribution is refused <br/><sub>Mahalanobis 16.0784 vs threshold 12.0</sub> | 422 out_of_distribution | 422 out_of_distribution | **PASS** |
| 11 | C1/C2 | A conformant calibration is accepted and scored per task | 201, four tasks sufficient | 201, 4/4 sufficient | **PASS** |
| 12 | D1/D2 | A session recording is accepted | 201 | 201 | **PASS** |
| 13 | D4-D8 | Segmentation produces bouts, with the doubtful ones flagged | 200, at least one bout | 200, 4 bouts, 3 flagged | **PASS** |
| 14 | E2/FR-07 | The review list is ordered least-certain first <br/><sub>A presentation requirement, not an API one. The segmentation endpoint returns bouts in temporal order -- which is what a timeline needs -- and the review ordering is applied by byReviewPriority() in frontend/src/lib/tasks.ts, unit-tested in tasks.test.ts (flagged first, ascending confidence, stable on ties). An earlier draft of this suite asserted it against the API and would have failed a requirement the API never carried.</sub> | Flagged bouts lead, then ascending confidence | not automated | **MANUAL** |
| 15 | E7/F1 | No metric is computed before a human approves | 412 segmentation_not_approved | 412 segmentation_not_approved | **PASS** |
| 16 | G1/E7 | Export is refused before approval too | 412 | 412 | **PASS** |
| 17 | E7 | An operator can approve the segmentation | 200 | 200 | **PASS** |
| 18 | F1/F4 | Metrics are computed once approved, with correction rate and confidence | 200, at least one task | 200, 3 task(s) | **PASS** |
| 19 | H1/FR-09 | The model card names both accuracy regimes | 200, transductive and causal both present | 200, both present: True | **PASS** |
| 20 | H2/FR-10 | Every inference records the model version that produced it <br/><sub>The artefact hash is recorded on the same session and published by the card as active_sha256 (4e68d8e64cb8...); SessionOut does not expose it, so the export PDF is where a clinician sees it.</sub> | A version on the segmented session, matching the served model card | session 'ensemble-1.0.0' vs card 'ensemble-1.0.0' | **PASS** |
| 21 | F3 | The intended-use statement appears on every screen and in the export <br/><sub>Visual; the API's own description carries it and the PDF embeds it. Checked by eye during the system walkthrough.</sub> | Present everywhere | not automated | **MANUAL** |
| 22 | I5/I6 | Responsive to 768 px, WCAG 2.1 AA contrast <br/><sub>Browser behaviour. Contrast is gated automatically by scripts/verify_palette.py on every push, including the timeline at its faintest.</sub> | Legible at three breakpoints; no contrast failure | not automated | **MANUAL** |
